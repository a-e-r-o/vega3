{
  "title": "VEGA Config",
  "type": "object",
  "properties": {
    "token": {
      "description": "Bot user's auth token",
			"type": "string",
			"minLength": 59,
			"maxLength": 59
		},
		"prefix": {
			"description": "Default prefix messages to be interpreted as commands",
			"type": "string"
		},
		"owner": {
			"description": "Bot owner's DiscordUser ID",
			"type": "string"
		}
  },
  "required": [ "token", "prefix" ]
}