//import { existsSync } from '../../deps.ts'

const basePath = './data'

export function write(key: string, data: string){
	//if (!existsSync(basePath))
	//Deno.mkdirSync(basePath)
	
	//Deno.writeTextFileSync(`${basePath}/${key}`, data)
}

export function read(key: string){
	//if (!existsSync(basePath))
	//Deno.mkdirSync(basePath)
	
	//return Deno.readTextFileSync(Deno.realPathSync(`${basePath}/${key}`))
}